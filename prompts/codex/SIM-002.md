# 任务 SIM-002：实现基础 PID 控制器

你是 Codex，本地代码代理，正在当前 Git 仓库中工作。

## 目标

实现一个简单、可复用的 PID baseline 控制器，用于后续水下机器人仿真对比实验。

## 需要创建或修改的文件

- `src/controllers/pid.py`
- `tests/test_pid.py`

## 要求

1. 实现类 `PIDController`。
2. 支持根据位置或标量误差输出简单控制量。
3. 包含比例、积分、微分三项参数。
4. 支持输出限幅。
5. 支持重置积分状态。
6. 代码保持轻量、清晰、易测试。

## 测试要求

请至少验证：

- 基本误差输入时能得到合理输出
- 限幅逻辑生效
- 重置后积分项清零
- 固定输入下行为确定

## 质量检查

运行：

```bash
pytest tests/test_pid.py -q
```

## 规则

- 不要修改无关文件
- 不要执行硬件相关任务
- 完成后更新 `progress.md`
- 测试通过后提交
