# 任务 FIG-001：创建轨迹绘图脚本模板

你是 OpenCode，本地代码代理，正在当前 Git 仓库中工作。

## 目标

创建一个可复用的轨迹绘图脚本，用于仿真结果展示。

## 文件

- 创建或修改：`src/visualization/plot_trajectory.py`
- 修改：`figures/README.md`

## 要求

1. 脚本应支持如下方式运行：

```bash
python src/visualization/plot_trajectory.py --help
```

2. 使用 `argparse`
3. 使用 `matplotlib`
4. 支持可选的 CSV 输入路径
5. 如果没有 CSV，则自动生成一条演示轨迹
6. 图像保存到：
   - `figures/trajectory_demo.png`
   - `figures/trajectory_demo.svg`
   - `figures/trajectory_demo.pdf`
7. 图中应包含坐标轴标签、图例、标题和等比例坐标

## 质量检查

运行：

```bash
python src/visualization/plot_trajectory.py
python src/visualization/plot_trajectory.py --help
```

## 规则

- 不要修改无关文件
- 更新 `progress.md`
- 成功后再提交
